"""
=============================================================
  app/core/__init__.py
=============================================================
📌 WHY THIS FILE EXISTS:
  Every folder in Python that you want to treat as a "package"
  (importable module) needs an __init__.py file.
  
  Without this file, Python won't recognize the folder as a
  package, and you'll get ImportError when trying to import
  from it.
  
  This file can be empty — its presence is what matters.
  But we can also use it to expose things at the package level.
"""
# This file intentionally left mostly empty.
# Its presence makes 'app/core/' a Python package.
