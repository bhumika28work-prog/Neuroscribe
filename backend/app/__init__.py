"""
=============================================================
  app/__init__.py
=============================================================
📌 WHY THIS FILE EXISTS:
  Makes the 'app/' directory a Python package.
  This allows other files to do: from app.core.config import settings
  
  Without this, Python treats 'app/' as just a regular folder,
  not a module you can import from.
"""
